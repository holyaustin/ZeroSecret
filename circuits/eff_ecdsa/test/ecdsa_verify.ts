describe("ecdsa_verify", () => {

  it("should verify the address and sig in email are valid", async () => {
    // const outputDir = setupDirectories(pathToCircom);

    // const circuit = await wasm_tester(
    //     path.join(__dirname, outputDir),
    //     {
    //         prime: "secq256k1"
    //     }
    // );

    // const circuitInput = generate input from eml

    // const w = await circuit.calculateWitness(circuitInput, true);

    // await circuit.checkConstraints(w);
  })
});
